%smth% stuff = ( %stuff% something);
if(testokay)
    for(smth;stuff;oops==true) {
        smth = (("st\nr)))${stuff ("inner")}ing"))
        stuff = function(orange, true) {
            % something is wrong %
            return 4
        }
        sutff = something * 4
        return 4
    }
else okay = 4;
for(smth in %comment% collection)
    %% one line comment
    test = 3
