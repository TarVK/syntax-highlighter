myFunction = function(val1, val2, val3) {
    return val1;
};

something = myFunction(45, myOtherFunction(false && true), "a string ${and(functio)}");
if(something)
    okay = true;

myFunction2 = (something)=>3 * something;

myFunction3 = (val1, val2) => {
    val1 = val1 * 3;
    return val1 + val2;
};